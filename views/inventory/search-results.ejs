<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | CSE Motors</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/inventory.css">
</head>
<body>
    <main>
        <!-- The content-container class will apply centering and width restrictions, 
             making the page look similar to the form pages. -->
        <div class="content-container search-results-view">
            
            <!-- The title variable is set in the searchController and includes the search term and result count. -->
            <h1><%= title %></h1>

            <!-- Check for local variables passed from the controller -->
            <% if (typeof search_term !== 'undefined' && search_term) { %>
                <p>We found the following results for your search: <strong>"<%= search_term %>"</strong></p>
            <% } %>

            <!-- Display any server-side errors, typically handled by the controller/utility -->
            <%- messages() %>

            <!-- The inventoryDisplay variable is the raw HTML generated by the buildInventoryGrid utility function in the controller. -->
             <div id="search-result-container">
                <%- inventoryDisplay %>
            </div>
        </div>
    </main>
</body>
</html>
